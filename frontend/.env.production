# =============================================================================
# Production build (used by `npm run build`)
# =============================================================================
# The SPA and the API share the platform origin (single domain + path routing,
# BLUEPRINT §32), so the API base is a relative path.
VITE_API_BASE_URL=/api/v1

VITE_DEFAULT_STORE_SLUG=demo
