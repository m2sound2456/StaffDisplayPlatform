# =============================================================================
# Staff Display Platform — backend environment template
# Copy to backend/.env (never committed) and fill in the real values.
# Configuration precedence: defaults < configs/config*.yaml < environment.
# =============================================================================

# --- Environment selection ---
# "production" switches to configs/config.production.yaml and strict validation.
APP_ENV=development

# --- Config file ---
# Explicit YAML path. Overrides the APP_ENV based file selection.
# CONFIG_PATH=

# --- Application ---
# APP_NAME=staffdisplay
# APP_TIMEZONE=Asia/Bangkok
# APP_SHUTDOWN_TIMEOUT=15s

# --- Server ---
# SERVER_HOST=0.0.0.0
# SERVER_PORT=8080
# SERVER_READ_TIMEOUT=15s
# SERVER_WRITE_TIMEOUT=30s
# SERVER_IDLE_TIMEOUT=60s

# --- Server TLS (leave disabled when nginx terminates TLS) ---
# SERVER_TLS_ENABLED=false
# SERVER_TLS_CERT_FILE=
# SERVER_TLS_KEY_FILE=

# --- Database ---
DATABASE_HOST=127.0.0.1
DATABASE_PORT=5432
DATABASE_USER=postgres
DATABASE_PASSWORD=CHANGE_ME
DATABASE_NAME=staffdisplay
DATABASE_SSLMODE=disable
DATABASE_TIMEZONE=Asia/Bangkok
# DATABASE_MAX_OPEN_CONNECTIONS=25
# DATABASE_MAX_IDLE_CONNECTIONS=5
# DATABASE_CONNECTION_MAX_LIFETIME=30m

# --- Auth (used from FG4) ---
# Generate a strong value for production: openssl rand -base64 48
AUTH_JWT_SECRET=CHANGE_ME
# AUTH_ACCESS_TOKEN_TTL=15m
# AUTH_REFRESH_TOKEN_TTL=720h

# --- CORS (comma separated origins) ---
# CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
